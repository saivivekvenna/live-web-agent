Web Agent to navigate a live browser while taking screen shots of UI states and where to click. 

Check `live_state_captures` folder to see what the prompt was and the live captures during runtime.

Check `dataset` to see comments over runtime outputs. 

Main Code is inside `agentB.py`